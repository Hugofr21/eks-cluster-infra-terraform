# eks-cluster-infra-terraform
Implements the infrastructure of an EKS cluster using Terraform, and may include modules, variables, state management, and associated network configurations (VPC, subnets, IAM roles, etc.).
